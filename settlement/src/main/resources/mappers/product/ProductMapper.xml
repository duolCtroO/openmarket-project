<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="oort.cloud.settlement.mapper.product.ProductMapper">
    <insert id="bulkInsert" parameterType="list">
        INSERT INTO products
        (user_id, product_name, description, price, stock, status, created_at)
        VALUES
        <foreach collection="products" item="p" separator=",">
        (#{p.userId}, #{p.productName}, #{p.description}, #{p.price}, #{p.stock}, #{p.status}, NOW())
        </foreach>
    </insert>

</mapper>